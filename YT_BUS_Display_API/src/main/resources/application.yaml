server:
  port: 82
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:DB/ytcx.db
mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
  configuration:
    cache-enabled: false
    local-cache-scope: statement